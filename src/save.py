def add_contact(name, number):
  # Save a new contact and return either True
  # or False indication the operation status
  status = True # False by default, return the appropriate boolean
  return status